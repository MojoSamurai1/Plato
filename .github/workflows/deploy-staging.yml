name: Deploy to Staging
on:
  push:
    branches: [staging]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.STAGING_SSH_KEY }}

      - name: Add host key
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p ${{ secrets.STAGING_SSH_PORT }} ${{ secrets.STAGING_SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy plugin to Staging
        run: |
          rsync -rlgoDzvc --delete \
            -e "ssh -p ${{ secrets.STAGING_SSH_PORT }}" \
            app/public/wp-content/plugins/plato-core/ \
            ${{ secrets.STAGING_SSH_USER }}@${{ secrets.STAGING_SSH_HOST }}:${{ secrets.STAGING_DEPLOY_PATH }}/wp-content/plugins/plato-core/

      - name: Deploy theme to Staging
        run: |
          if [ -d "app/public/wp-content/themes/plato" ]; then
            rsync -rlgoDzvc --delete \
              -e "ssh -p ${{ secrets.STAGING_SSH_PORT }}" \
              app/public/wp-content/themes/plato/ \
              ${{ secrets.STAGING_SSH_USER }}@${{ secrets.STAGING_SSH_HOST }}:${{ secrets.STAGING_DEPLOY_PATH }}/wp-content/themes/plato/
          fi

      - name: Deployment Complete
        run: |
          echo "Deployed to staging"
          echo "Commit: ${{ github.sha }}"
